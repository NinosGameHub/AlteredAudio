**Badge** — a small uppercase pill for faceplate metadata.

```jsx
<Badge tone="amber">MOD</Badge>
<Badge tone="outline">001</Badge>
<Badge tone="display">4x</Badge>
```

Tones: `amber` (filled accent), `outline` (hairline), `display` (dark amber-text well), `warning` (orange).
